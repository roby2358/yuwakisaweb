// R2-hosted Z3 WebAssembly URL. See SPEC.md "Setup (R2)" for how to
// provision this bucket and set the value below. Non-secret — committed
// to the repo so the page can locate the WASM on first load.
export const Z3_WASM_URL = 'https://pub-3de05a81a5c94b69ac5dbc091eff370a.r2.dev/z3-built.wasm';
